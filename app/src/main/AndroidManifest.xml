<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.taspen.kamus" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_kamus_taspen"
        android:label="@string/app_name"
        android:theme="@style/Theme.HighlightCopy" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DaftarAbjadActivity"
            android:label="@string/app_name"/>
        <activity android:name=".DaftarIstilahByKataActivity"
            android:label="@string/app_name"/>
        <activity android:name=".MainActivity"
            android:label="@string/app_name"/>
        <activity android:name=".KataDetailVersionOneActivity"
            android:label="@string/app_name"/>
        <activity android:name=".SyaratPengajuanClaimActivity"
            android:label="@string/app_name"/>
        <activity android:name=".InformasiActivity"
            android:label="@string/app_name"/>
        <activity android:name=".DaftarPersyaratanDetailActivity"
            android:label="@string/app_name"/>
        <activity android:name=".AboutActivity"
            android:label="@string/app_name"/>
    </application>

</manifest>
